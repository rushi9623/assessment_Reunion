import React from 'react';
import './Sidebar.css';

function Sidebar({ columns, toggleColumn }) {
  return (
    <div className="sidebar">
      {/* Sidebar content here */}
    </div>
  );
}

export default Sidebar;
